const defaultState = {
    socket: null,
    message: '',
    thread: [],
    currentThread: ''
}

const chat = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default chat;